#include <stdio.h>

int main() {

    int preco1 = 50+50+50;
    int preco2 = 166;

if (preco1 > preco2)
    printf("Preço 1 é maior que preço 2");

    else
    if (preco1 < preco2)
    printf("Preço 2 é maior que preço 1");
    else 
    printf("Os preços são iguais");

    return 0;
}
